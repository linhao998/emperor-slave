import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.linhao.emperorslave',
  appName: 'emperor-slave-game',
  webDir: 'dist'
};

export default config;
